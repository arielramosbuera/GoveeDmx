{
	"name": "@goveedmx/server",
	"version": "1.0.2",
	"private": true,
	"description": "GoveeDMX backend: Art-Net listener, mapping engine, Govee LAN control, REST + WebSocket API",
	"license": "GPL-3.0-or-later",
	"main": "dist/server.cjs",
	"scripts": {
		"build": "node build.mjs",
		"dev": "tsx watch src/server.ts",
		"start": "node dist/server.cjs",
		"test": "vitest run",
		"test:watch": "vitest",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"ws": "^8.18.0",
		"zod": "^3.24.1"
	},
	"devDependencies": {
		"@types/node": "^22.10.0",
		"@types/ws": "^8.5.13",
		"esbuild": "^0.24.0",
		"tsx": "^4.19.2",
		"typescript": "^5.7.2",
		"vitest": "^2.1.8"
	}
}
