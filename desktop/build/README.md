# Desktop build resources

Place app icons here for branded installers:

- `icon.ico` — Windows (256×256 recommended)
- `icon.icns` — macOS
- `icon.png` — Linux (512×512)

electron-builder picks these up automatically. Without them, the default Electron icon is used.
